name: "📋 AI Scheduled Issue Triage"

on:
  schedule:
    - cron: "0 0 * * *" # Runs daily at midnight
  pull_request:
    branches:
      - "main"
      - "release"
      - "release/**/*"
  push:
    branches:
      - "main"
      - "release"
      - "release/**/*"
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}"
  cancel-in-progress: true

defaults:
  run:
    shell: "bash"

env:
  SENTRY_DSN: ${{ secrets.SENTRY_DSN }}

jobs:
  triage:
    runs-on: "blacksmith-4vcpu-ubuntu-2404"
    timeout-minutes: 7
    permissions:
      contents: "read"
      id-token: "write"
      issues: "read"
      pull-requests: "read"
    outputs:
      available_labels: "${{ steps.get_labels.outputs.available_labels }}"
      triaged_issues: "${{ steps.parse_output.outputs.triaged_issues }}"
    steps:
      - uses: actions/checkout@v4
      - name: "Get repository labels"
        id: "get_labels"
        uses: "actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea" # ratchet:actions/github-script@v7.0.1
        with:
          # NOTE: we intentionally do not use the minted token. The default
          # GITHUB_TOKEN provided by the action has enough permissions to read
          # the labels.
          script: |-
            const labels = [];
            for await (const response of github.paginate.iterator(github.rest.issues.listLabelsForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              per_page: 100, // Maximum per page to reduce API calls
            })) {
              labels.push(...response.data);
            }

            if (!labels || labels.length === 0) {
              core.setFailed('There are no issue labels in this repository.')
            }

            const labelNames = labels.map(label => label.name).sort();
            core.setOutput('available_labels', labelNames.join(','));
            core.info(`Found ${labelNames.length} labels: ${labelNames.join(', ')}`);
            return labelNames;

      - name: "Find untriaged issues"
        id: "find_issues"
        env:
          GITHUB_REPOSITORY: "${{ github.repository }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN || github.token }}"
        run: |-
          echo '🔍 Finding unlabeled issues and issues marked for triage...'
          ISSUES="$(gh issue list \
            --state 'open' \
            --search 'no:label' \
            --json number,title,body \
            --limit '100' \
            --repo "${GITHUB_REPOSITORY}"
          )"

          echo '📝 Setting output for GitHub Actions...'
          echo "issues_to_triage=${ISSUES}" >> "${GITHUB_OUTPUT}"

          ISSUE_COUNT="$(echo "${ISSUES}" | jq 'length')"
          echo "✅ Found ${ISSUE_COUNT} issue(s) to triage! 🎯"

      - name: "Run AI Issue Analysis"
        id: "ai_issue_analysis"
        if: |-
          ${{ steps.find_issues.outputs.issues_to_triage != '[]' }}
        env:
          # Prefer wiring to existing secret names without renaming secrets.
          # The CLI expects KILOCODE_TOKEN, but some repos store this as KILOCODE_API_KEY.
          KILOCODE_TOKEN: "${{ secrets.KILOCODE_TOKEN || secrets.KILOCODE_API_KEY }}"
          KILOCODE_MODEL: "${{ vars.KILOCODE_MODEL }}"
          KILOCODE_POSTHOG_API_KEY: "${{ secrets.KILOCODE_POSTHOG_API_KEY }}"
          ISSUES_TO_TRIAGE: "${{ steps.find_issues.outputs.issues_to_triage }}"
          AVAILABLE_LABELS: "${{ steps.get_labels.outputs.available_labels }}"
        run: |
          # Generate Kilocode Configuration
          cat <<'EOF' > kilocode.config.json
          {
            "version": "1.0.0",
            "mode": "code",
            "telemetry": true,
            "provider": "default",
            "providers": [
              {
                "id": "default",
                "provider": "kilocode",
                "kilocodeToken": "${KILOCODE_TOKEN}",
                "kilocodeModel": "${KILOCODE_MODEL}",
                "kilocodePosthogApiKey": "${KILOCODE_POSTHOG_API_KEY}"
              }
            ],
            "autoApproval": {
              "enabled": true,
              "read": {
                "enabled": true,
                "outside": false
              },
              "write": {
                "enabled": true,
                "outside": true,
                "protected": false
              },
              "browser": {
                "enabled": true
              },
              "retry": {
                "enabled": true,
                "delay": 10
              },
              "mcp": {
                "enabled": true
              },
              "mode": {
                "enabled": true
              },
              "subtasks": {
                "enabled": true
              },
              "execute": {
                "enabled": true,
                "allowed": [
                  "ls",
                  "cat",
                  "echo",
                  "pwd",
                  "mkdir",
                  "cd",
                  "kubectl",
                  "curl"
                ],
                "denied": [
                  "rm -rf",
                  "sudo rm",
                  "mkfs",
                  "dd if="
                ]
              },
              "question": {
                "enabled": false,
                "timeout": 60
              },
              "todo": {
                "enabled": true
              }
            },
            "theme": "dark",
            "customThemes": {}
          }
          EOF

          # Loop through issues one by one for maximum robustness
          RESULTS="[]"
          echo "🔍 Triaging issues one by one..."

          # Use jq to get issues safely
          ISSUES_JSON=$(echo "$ISSUES_TO_TRIAGE" | jq -c '.[]')

          while IFS= read -r ISSUE; do
            NUMBER=$(echo "$ISSUE" | jq -r '.number')
            TITLE=$(echo "$ISSUE" | jq -r '.title')
            echo "🎯 Triaging issue #$NUMBER: $TITLE"

            PROMPT="You are a GitHub issue triager. Available labels: $AVAILABLE_LABELS.
            Triage issue #$NUMBER. Title: $TITLE.
            Return ONLY a valid JSON object: {\"labels_to_set\": [\"label1\", \"label2\"]}. ALL strings must be double-quoted."

            echo "🔄 Triaging issue #$NUMBER with Kilocode AI..."
            # Ensure local kilocode CLI is usable
            if [ -f scripts/kilocode-cli.cjs ]; then
              chmod +x scripts/kilocode-cli.cjs
              sudo ln -sf "$(pwd)/scripts/kilocode-cli.cjs" /usr/local/bin/kilocode
            fi
            if ! RESPONSE_RAW=$(kilocode "$PROMPT" 2>cli_error.log); then
              echo "⚠️ Failed to triage #$NUMBER"
              echo "📝 Error details:"
              cat cli_error.log
              echo "::group::Debug Information for Issue #$NUMBER"
              echo "Prompt: $PROMPT"
              echo "::endgroup::"
              continue
            fi
            echo "✅ Successfully triaged issue #$NUMBER"

            # Extract labels safely. We handle potential non-JSON noise from the AI.
            LABELS=$(echo "$RESPONSE_RAW" | sed -n '/{/,/}/p' | jq -c '.labels_to_set' 2>/dev/null || echo "[]")
            
            # Ensure LABELS is a valid JSON array
            if [[ ! "$LABELS" =~ ^\[.*\]$ ]]; then
              LABELS="[]"
            fi

            # Safely append to RESULTS using --argjson to prevent shell parsing/syntax issues
            RESULTS=$(echo "$RESULTS" | jq -c --argjson num "$NUMBER" --argjson labs "$LABELS" '. + [{"issue_number": $num, "labels_to_set": $labs}]')

          done <<< "$ISSUES_JSON"

          echo "triaged_issues<<EOF" >> $GITHUB_OUTPUT
          echo "$RESULTS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: "Parse AI output"
        id: "parse_output"
        run: |
          # Use output from previous step directly
          echo "triaged_issues<<EOF" >> $GITHUB_OUTPUT
          echo '${{ steps.ai_issue_analysis.outputs.triaged_issues }}' >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  label:
    runs-on: "blacksmith-4vcpu-ubuntu-2404"
    needs:
      - "triage"
    if: |-
      needs.triage.outputs.available_labels != '' &&
      needs.triage.outputs.triaged_issues != ''
    permissions:
      contents: "read"
      issues: "write"
      pull-requests: "write"
    steps:
      - name: "Mint identity token"
        id: "mint_identity_token"
        if: |-
          ${{ vars.APP_ID }}
        uses: "actions/create-github-app-token@a8d616148505b5069dccd32f177bb87d7f39123b" # ratchet:actions/create-github-app-token@v2
        with:
          app-id: "${{ vars.APP_ID }}"
          private-key: "${{ secrets.APP_PRIVATE_KEY }}"
          permission-contents: "read"
          permission-issues: "write"
          permission-pull-requests: "write"

      - name: "Apply labels"
        env:
          AVAILABLE_LABELS: "${{ needs.triage.outputs.available_labels }}"
          TRIAGED_ISSUES: "${{ needs.triage.outputs.triaged_issues }}"
        uses: "actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea" # ratchet:actions/github-script@v7.0.1
        with:
          # Use the provided token so that the "kilocode-cli" is the actor in the
          # log for what changed the labels.
          github-token: "${{ steps.mint_identity_token.outputs.token || secrets.GITHUB_TOKEN || github.token }}"
          script: |-
            // Parse the available labels
            const availableLabels = (process.env.AVAILABLE_LABELS || '').split(',')
              .map((label) => label.trim())
              .sort()

            // Parse out the triaged issues safely
            const raw = JSON.parse(process.env.TRIAGED_ISSUES || '[]');
            const triagedIssues = (Array.isArray(raw) ? raw : (raw.issues || []))
              .sort((a, b) => a.issue_number - b.issue_number)

            core.debug(`Triaged issues: ${JSON.stringify(triagedIssues)}`);

            // Iterate over each label
            for (const issue of triagedIssues) {
              if (!issue) {
                core.debug(`Skipping empty issue: ${JSON.stringify(issue)}`);
                continue;
              }

              const issueNumber = issue.issue_number;
              if (!issueNumber) {
                core.debug(`Skipping issue with no data: ${JSON.stringify(issue)}`);
                continue;
              }

              // Extract and reject invalid labels - we do this just in case
              // someone was able to prompt inject malicious labels.
              let labelsToSet = (issue.labels_to_set || [])
                .map((label) => label.trim())
                .filter((label) => availableLabels.includes(label))
                .sort()

              core.debug(`Identified labels to set: ${JSON.stringify(labelsToSet)}`);

              if (labelsToSet.length === 0) {
                core.info(`Skipping issue #${issueNumber} - no labels to set.`)
                continue;
              }

              core.debug(`Setting labels on issue #${issueNumber} to ${labelsToSet.join(', ')} (${issue.explanation || 'no explanation'})`)

              await github.rest.issues.setLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: labelsToSet,
              });
            }
