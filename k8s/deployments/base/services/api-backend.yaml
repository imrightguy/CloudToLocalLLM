apiVersion: v1
kind: Service
metadata:
  name: api-backend
  namespace: cloudtolocalllm
  labels:
    app: api-backend
    environment: development
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 3000
      name: http
  selector:
    app: api-backend