apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: zoidbot
  labels:
    app: web
    environment: development
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: web