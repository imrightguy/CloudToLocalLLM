apiVersion: v1
kind: Service
metadata:
  name: streaming-proxy
  namespace: CloudToLocalLLM
  labels:
    app: streaming-proxy
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: streaming-proxy
