apiVersion: v1
kind: Service
metadata:
  name: api-backend
  namespace: zoidbot
  labels:
    app: api-backend
    environment: development
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: api-backend